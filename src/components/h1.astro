---
import clsx from "clsx/lite";

type Props = {
    className?: string;
    size?: "default" | "large";
}

const { size = "default", className } = Astro.props;
---

<h1
    class={clsx(
        "text-white font-bold text-balance",
        size === "default" && "text-4xl md:text-5xl xl:text-7xl tracking-tight",
        size === "large" && "text-6xl md:text-[80px] lg:text-[100px] tracking-tighter",
        className,
    )}
>
    <slot />
</h1>
